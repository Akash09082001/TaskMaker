/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'images.unsplash.com',
            'cdni.iconscout.com'
        ],
    },
};

export default nextConfig;
